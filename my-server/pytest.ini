[pytest]
markers =
    integration: marks tests as hitting real external services (deselect with -m "not integration")
    timeout: marks tests with a timeout value in seconds (requires pytest-timeout)
